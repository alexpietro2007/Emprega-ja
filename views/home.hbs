<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Home</title>
  <link rel="stylesheet" href="/CSS/home.css">
</head>
<body>
  <header>
    <h1>Bem-vindo, {{usuario.nome}}!</h1>
    <p>Tipo de usuário: {{usuario.tipo}}</p>
    <nav>
      <a href="/cadastro/trabalhador/informacoes-pessoais">Sair</a>
    </nav>
  </header>

  <main>
    {{#if (eq usuario.tipo "trabalhador")}}
      <p>Área do trabalhador</p>
    {{/if}}

    {{#if (eq usuario.tipo "empresa")}}
      <p>Área da empresa</p>
    {{/if}}

    {{#if (eq usuario.tipo "admin")}}
      <p>Área de administração</p>
    {{/if}}
  </main>
</body>
</html>
